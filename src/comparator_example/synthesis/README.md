# Synthesis

To run synthesis:

- edit `scripts/config.tcl` with your design references
- run `make syn`

- If you got an error, and the terminal is in the genus synthesis tool enter `quit` to quit.

## Features

- You can clean your environment with `make clean`
- you can track some synthesis KPIs with `synthesis.csv` file
